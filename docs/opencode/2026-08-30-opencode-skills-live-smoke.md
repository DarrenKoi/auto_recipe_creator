# OpenCode 스킬 라이브 스모크 테스트 — opencode oc-review 기록

- 실행일: 2026-08-30
- 스킬: oc-review, oc-discuss
- 모델: opencode-go/kimi-k3 (tier=heavy)
- 대상: OpenCode 연결, 응답, 세션 연속성 및 현재 저장소 호환성
- 소요: 20초 + 15초 · 세션: ses_fb0ad380fffeBzWCaPxyBN5web

## 모델 응답

> OPENCODE_OK

> SESSION_OK

## 판단

공유 실행기는 실제 공급자 호출과 동일 세션 후속 호출에 성공했습니다.
세 스킬의 정적 구조 검사와 실행기 셸 문법 검사도 통과했습니다.

저장소 파일을 읽는 두 축 리뷰는 샌드박스에서 공급자 오류로 실패했습니다.
저장소와 AGENTS.md 내용을 외부 공급자에 전송하는 승격 실행은 별도 사용자
승인이 없어 차단되었으므로, 실제 저장소 읽기 품질은 이번 실행에서 검증하지
않았습니다.

현재 스킬 문서에는 SKEWNONO 전용 경로, 프롬프트, npm 및 `.venv` 명령이
남아 있습니다. 따라서 실행기 연결은 정상이나 `auto_recipe_creator`에서 세
스킬이 그대로 잘 작동한다고 판정할 수 없습니다.

## 후속

스킬 수정은 요청 범위가 아니므로 적용하지 않았습니다. 저장소 내용을 외부
OpenCode 공급자에 보내는 실제 리뷰는 명시적 승인 후 다시 실행해야 합니다.
